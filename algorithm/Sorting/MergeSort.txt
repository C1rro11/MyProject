MergeSort
purpose: 
1: divide a whole array into half
2: sort each half recursively.
3: make 2 half merge tgt.

implementation:

MergeSort(A,p,r):
    if p = r then return
    q = [(p+r)/2]
    MergeSort(A,p,q)
    MergeSort(A,q+1,r)
    Merge(A,p,q,r)


first call MergeSort(A,1,n)